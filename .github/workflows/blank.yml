name: Dummy Check
on:
  pull_request:
    branches: [ "main" ]
  merge_group: # <--- Required for the merge queue to process it

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Queue check passed!"
